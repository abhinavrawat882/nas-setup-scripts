# RabbitMQ config for NAS projects stack.
# No tight absolute memory watermark — home workloads on 32GB may use RAM freely.
# Durability comes from the bind-mounted data dir + durable/quorum queues in clients.

# Stop accepting publishes before the NAS disk fills (protects on-disk messages).
disk_free_limit.absolute = 2GB
